using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddMeToBallonCounter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PlatformsOps.AddOneToBalloonCounter();
    }
}
